from pathlib import Path

root = str(Path.cwd()) + "\\"
credentials = root + "auth_creds.json"
reports = root + "reports"
screenshots = root + "screenshots"
selenium_prefs = root + "selenium_prefs.json"
pytest_logs = root + "test.log"
